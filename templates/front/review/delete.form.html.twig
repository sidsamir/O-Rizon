<form method="post" action="{{ path('app_front_review_delete', {'id': review.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review.id) }}">
    <div class="flex justify-center items-center">
		<button class="flex center-item bg-green-500 hover:bg-green-700 text-white py-2 px-4 rounded-full mt-4 mb-2 text-xl pl-20 pr-20">Supprimer</button>
	</div>
</form>
